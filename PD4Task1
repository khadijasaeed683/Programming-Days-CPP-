#include <iostream>
#include <windows.h>
using namespace std;

int main()
{
    cout << "Enter a number (1-99): ";
    int digit;
    cin >> digit;
    digit 
    remainder= digit/2
    if (digit==1){
        cout <<"One" ;
    }
    else if (digit==2) {
        cout << "Two";
    }
   else if (digit==3) {
    cout <<"Three";

   }
   else if (digit==4) {
    cout <<"Four";
     }
      else if (digit==5) {
    cout <<"Five";
     }
      else if (digit==6) {
    cout <<"Six";
     }
      else if (digit==7) {
    cout <<"Seven";
     }
    return 0;

}
